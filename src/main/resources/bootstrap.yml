server:
  port: 8081
spring:
  application:
    name: microservice-template
  cloud:
    kubernetes:
      config:
        enabled: false
        name: ${spring.application.name}-config
      reload:
        enabled: false
keycloak:
  enabled: false
